mutation Register($input: RegistrationInputType!) {
    register(input: $input) {
        id
        created_at
        updated_at
        email
        phone
        name
        nick_name
        avatar_url
        claims
        status
    }
}